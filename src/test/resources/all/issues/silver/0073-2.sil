field f: Int

method test() {
  //field access in triggers are only allowed under certain conditions; check in Silicon produces internal error
  //:: ExpectedOutput(internal:feature.unsupported)
  inhale forall x: Ref :: {x.f} true
}
