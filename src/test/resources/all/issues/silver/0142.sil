// Any copyright is dedicated to the Public Domain.
// http://creativecommons.org/publicdomain/zero/1.0/

method helper(a:Int, b: Int)
{
    assert -a+b == (-a) + b    
    //:: ExpectedOutput(assert.failed:assertion.false)
    assert -a+b == -(a + b)    
}
