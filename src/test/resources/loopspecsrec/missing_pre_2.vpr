// decl out, assigned inside -> i
//missing pre as while loop doesn't have this info

// 1. Neg Case V2

// pos and neg cases for this and ensures pre(i) < i
// experiment with bc and without
method f(l : Ref)
    requires l != null
   {
   var curr : Ref := l

   var i : Int := 1

   while(curr != null)
   //:: ExpectedOutput(postcondition.not.preserved.inductive.step:assertion.false)
       ensures curr != null ==> pre(i) + 1 == i
   {

        curr := null
        i := i + 1
   }ghost{
        i := i + 1
   }
   
}

