// Any copyright is dedicated to the Public Domain.
// http://creativecommons.org/publicdomain/zero/1.0/

define macro(x, y) x + macro2(y, x)

define macro2(z, x) x + macro3(z, z)

define macro3(a, b) a * b

method main()
{
    while(macro(1, 4) == 18) {
        
    }
    assert(false)
}

