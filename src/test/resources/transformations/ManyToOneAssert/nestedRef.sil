method main()
{
    var b: Bool := true
    var c: Bool := false
    assert(true && true)
    while(b == true) {
        assert(true && b)
        if(c == true) {
            assert(b && c)
        }
    }
}