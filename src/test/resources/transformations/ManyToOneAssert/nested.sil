method main()
{
    var b: Bool := true
    var c: Bool := false
    assert(true)
    assert(true)
    while(b == true) {
        assert(true)
        assert(b)
        if(c == true) {
            assert(b)
            assert(c)
        }
    }
}