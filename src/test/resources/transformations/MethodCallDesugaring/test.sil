field f:Int

method main()
{
    var a: Ref
    a := new(f)
    //a.f := 5
    //foo(a)
}

/*method foo(x: Ref)
    requires acc(x.f) && x.f == 5
    ensures acc(x.f, wildcard)
{
    x.f := 4
}*/