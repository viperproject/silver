import "../decreaseOrder/int_decreases.sil"

domain DecreasesDomainX[T]{
    function decreasesX(t: T): Bool
}

function test(): Int
    ensures decreases(1)
{
    1
}

function test2(x: Int): Int
    ensures decreases(x, x+x, x)
{
    2
}