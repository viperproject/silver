import "../decreaseOrder/dec.vpr"

domain BoolTerminationOrder{
    //Booleans
    axiom bool_ax_dec{
        forall bool1: Bool ::
        bool1 != false ==> decreasing(bool1, false)
    }
    axiom bool_ax_bound{
        forall bool1: Bool :: 
        bounded(bool1)
    }
}