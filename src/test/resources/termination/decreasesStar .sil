import "decreaseOrder/int_decreases.vpr"

function f(x:Int): Int
requires x > 0
decreases x
{

    x<5 ? f(x-3) : g(x-1)
}

function g(x:Int): Int
requires x > 0
{
    x< 9 ? f(x-1) : h(x-2)
}

function h(x:Int): Int
requires x > 0
decreases *
{
    x < 9 ? h(x) : f(x)
}