predicate Pointer(ref: Ref)

method writeTo(ptr: Ref, value: Int)
  requires acc(Pointer(ptr), 1 / 1)
  requires perm'(Pointer(ptr)) == 2 / 1 // BAD, should only be 1
{
}

method client(ref: Ref)
  requires acc(Pointer(ref), 1/1) {
  call writeTo(ref, 1)
}
