//:: IgnoreFile(/silicon/issue/000000000000000/)
field b: Bool

predicate Cell(x: Ref) {
    acc(x.b)
}

method test0(x: Ref)
requires Cell(x)
{
    package Cell(x) --* false {
        unfold Cell(x)
        //:: ExpectedOutput(unfold.failed:insufficient.permission)
        unfold Cell(x)
    }
}