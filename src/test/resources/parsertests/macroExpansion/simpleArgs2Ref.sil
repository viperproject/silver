define macro(x, y) {
    assert(x + y == 5)
    macro2(y)
}

define macro2(z) {
    assert(z == 3)
}

method main()
{
    assert(true)
    assert(1+1 + (1+2) == 5)
    assert(1+2 == 3)
}

