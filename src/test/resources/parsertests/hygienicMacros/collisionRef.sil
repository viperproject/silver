define macro {
    var x: Bool := true
    assert(x)
}

method x$0()

method x$1()

function x$3() : Bool {
    true
}

method main()
{
    var x: Bool
    var x$2: Bool
    x := true
    assert(x)
    x$2 := true
    assert(x$2)
}