define macro {
    var x: Bool := true
    macro2(true && true, x)
}

define macro2(x, y) {
    assert(x && y)
}

method main()
{
    var x$0: Bool
    var x$1: Bool
    x$0 := true
    assert((true && true) && x$0)
    x$1 := true
    assert((true && true) && x$1)
}