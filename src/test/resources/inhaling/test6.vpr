field f: Int

method test6a(x: Ref)
    requires acc(x.f)
{
    assert inhaling (acc(x.f)) in true
    assert false
}

method test6b(x: Ref)
    requires acc(x.f)
{
    assert inhaling (acc(x.f)) in false
}
