import "size_order.vpr"

domain TerminationOrder[E]{
    function decreases(arg1:E, arg2:E):Bool

    axiom decreasing_sizes{
        forall e1:E, e2:E ::
        size(e1) >= 0 && size(e1) < size(e2) ==> decreases(e1,e2)
    }
}